import 'package:flutter/material.dart';

class StateScreensPreviewPage extends StatelessWidget {
  const StateScreensPreviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Center(
          child: Text('State preview is temporarily unavailable.'),
        ),
      ),
    );
  }
}
